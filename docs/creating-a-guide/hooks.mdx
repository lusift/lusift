---
id: hooks
title: 'Hooks'
description: Using hooks
---

Hooks run at different points in the active guide's lifecycle.

## onNext()
`onNext()` runs on `Lusift.next()`

## onPrev()
`onPrev()` runs on `Lusift.prev()`

## onClose()
`onClose()` runs on `Lusift.close()`
